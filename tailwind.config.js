/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      // ==========================================================
      // 颜色 —— 严格分层：中性 / 品牌 / 语义
      // 使用原则：
      //   - 页面 95% 的面积使用中性色
      //   - 品牌色只用于主 CTA / 链接 / 选中态，面积 < 5%
      //   - 语义色只用于状态反馈（成功 / 警告 / 错误）
      // ==========================================================
      colors: {
        bg: {
          DEFAULT: '#FFFFFF',      // 主背景
          subtle: '#F8F9FC',       // 次级背景（section、列表容器）
          muted: '#F1F3F7',        // hover 背景
        },
        border: {
          DEFAULT: '#E5E9EF',      // 默认边框
          strong: '#CBD2DC',       // 强调边框、input focus
        },
        text: {
          primary: '#1E2330',      // 标题、正文
          secondary: '#5A6473',    // 次级文字、表单标签
          tertiary: '#8B93A1',     // meta、placeholder、disabled
        },
        brand: {
          50:  '#EEF2FF',          // 选中态背景
          100: '#E0E7FF',
          500: '#4F46E5',          // 主 CTA
          600: '#4338CA',          // hover
          700: '#3730A3',
        },
        success: {
          DEFAULT: '#10B981',
          bg: '#ECFDF5',
        },
        warning: {
          DEFAULT: '#F59E0B',
          bg: '#FFFBEB',
        },
        danger: {
          DEFAULT: '#EF4444',
          bg: '#FEF2F2',
        },
      },

      // ==========================================================
      // 字号 —— 正文 15px（而非 16px），更工具感
      // ==========================================================
      fontSize: {
        xs:   ['12px', { lineHeight: '16px' }],
        sm:   ['14px', { lineHeight: '20px' }],
        base: ['15px', { lineHeight: '22px' }],
        lg:   ['17px', { lineHeight: '24px' }],
        xl:   ['20px', { lineHeight: '28px' }],
        '2xl':['24px', { lineHeight: '32px' }],
        '3xl':['32px', { lineHeight: '40px' }],
      },

      // ==========================================================
      // 字重 —— 只用三档
      // 禁用 bold(700)
      // ==========================================================
      fontWeight: {
        normal: '400',
        medium: '500',
        semibold: '600',
      },

      // ==========================================================
      // 圆角
      // ==========================================================
      borderRadius: {
        sm:   '6px',    // tag、小按钮
        md:   '8px',    // input、button
        lg:   '12px',   // card
        xl:   '16px',   // modal、大卡片
        pill: '9999px', // 筛选 tab、徽章、头像
      },

      // ==========================================================
      // 阴影 —— 只用三档
      // ==========================================================
      boxShadow: {
        sm: '0 1px 2px rgba(16, 24, 40, 0.04)',
        md: '0 4px 12px rgba(16, 24, 40, 0.06)',
        lg: '0 12px 32px rgba(16, 24, 40, 0.08)',
      },

      // ==========================================================
      // 字体
      // ==========================================================
      fontFamily: {
        sans: ['Inter', 'system-ui', '-apple-system', 'sans-serif'],
        mono: ['"JetBrains Mono"', 'ui-monospace', 'monospace'],
      },

      // ==========================================================
      // 容器最大宽度
      // ==========================================================
      maxWidth: {
        '6xl': '1152px',
      },
    },
  },
  plugins: [],
};
